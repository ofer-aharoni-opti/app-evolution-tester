# Open questions

- [2026-05-03 PR#—] Datadog APM in [Dockerfile](../Dockerfile) sets `CORECLR_*` and `LD_PRELOAD` paths under `/app/datadog/...` and runs `sh /app/datadog/createLogPath.sh`, but no step in the Dockerfile copies those files in. Tried: read full Dockerfile, ci.yml, docker-compose.local.yml, README. No reference to a base image layer or build step that injects the Datadog agent. Question: is this expected to be added by a downstream build pipeline, or is APM effectively dormant in the current image?
- [2026-05-03 PR#—] Two integration test factories coexist: [tests/Template.IntegrationTests/IntegrationWebApplicationFactory.cs](../tests/Template.IntegrationTests/IntegrationWebApplicationFactory.cs) and [tests/Template.IntegrationTests/Infrastructure/IntegrationWebApplicationFactory.cs](../tests/Template.IntegrationTests/Infrastructure/IntegrationWebApplicationFactory.cs). Tried: file listing, no PR explains the duplication. Question: which one is canonical, and should the other be deleted?
